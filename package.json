{
  "name": "speak-page-actions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:site": "vite --config vite.site.config.ts",
    "build:extension": "wxt build && rm -rf dist/extension && mkdir -p dist/extension && cp -R .output/chrome-mv3/. dist/extension/ && mkdir -p dist/site/downloads && cd dist/extension && zip -qr ../site/downloads/speak-page-actions.zip .",
    "build:site": "vite build --config vite.site.config.ts",
    "build": "npm run build:site && npm run build:extension",
    "test": "npm run test:unit && npm run test:claims",
    "test:unit": "vitest run tests/actions.test.ts",
    "test:claims": "playwright test",
    "preview": "vite preview --config vite.site.config.ts --host 127.0.0.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "@playwright/test": "1.58.2",
    "@types/chrome": "^0.1.31",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.7",
    "wxt": "^0.20.0"
  }
}
