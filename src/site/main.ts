import './style.css';
import './touch.css';

type DemoAction = { label: string; kind: string; destructive?: boolean; done?: boolean };
const sampleActions: DemoAction[] = [
  { label: 'Save address', kind: 'button' }, { label: 'Shipping method', kind: 'field' }, { label: 'Review order', kind: 'link' }, { label: 'Delete saved draft', kind: 'button', destructive: true }
];
const app = document.querySelector<HTMLDivElement>('#app')!;
const base = 'https://speak-page-actions.sociobot.in';
const route = () => new URLSearchParams(location.search).get('demo') === '1' ? '/demo' : location.pathname.replace(/\/$/, '') || '/';
const escapeHtml = (value: string) => value.replace(/[&<>'"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[char]!));
function setMeta(title: string, description: string, canonical = route()) {
  document.title = title;
  document.querySelector('meta[name="description"]')?.setAttribute('content', description);
  document.querySelector('link[rel="canonical"]')?.setAttribute('href', `${base}${canonical}`);
  for (const [selector, value] of [['meta[property="og:title"]', title], ['meta[name="twitter:title"]', title], ['meta[property="og:description"]', description], ['meta[name="twitter:description"]', description], ['meta[property="og:image"]', `${base}/art/social.webp`], ['meta[name="twitter:image"]', `${base}/art/social.webp`]]) document.querySelector(selector)?.setAttribute('content', value);
}
function shell(content: string, active = '') {
  return `<a class="skip" href="#main">Skip to main content</a><header class="site-header"><a class="wordmark" href="/" aria-label="Speak Page Actions home"><span aria-hidden="true">●</span> Speak Page Actions</a><nav aria-label="Main navigation"><a href="/demo" ${active === 'demo' ? 'aria-current="page"' : ''}>Demo</a><a href="/downloads/speak-page-actions.zip" download>Desktop download</a><a href="/privacy" ${active === 'privacy' ? 'aria-current="page"' : ''}>Privacy</a></nav></header>${content}<footer><p>Speak visible page actions with review before sensitive clicks.</p><nav aria-label="Footer navigation"><a href="/privacy">Privacy</a><a href="/terms">Terms</a><span>Built by Param Factory</span></nav><small>v1.0.0 · Original generated illustration disclosed above.</small></footer><div id="route-note" class="sr-only" aria-live="polite"></div>`;
}
function home() {
  const returnedLicense = new URLSearchParams(location.search).get('license');
  setMeta('Speak Page Actions — Speak visible page actions', 'Speak visible page actions with review before sensitive clicks.');
  app.innerHTML = shell(`<main id="main" tabindex="-1"><section class="hero"><div class="hero-copy"><p class="kicker">Browser controls you can use by voice</p><h1>Speak the action you need</h1><p class="lede">For people with limited vision or hand mobility, say or type a visible control’s name.</p><div class="hero-actions"><a class="button primary" href="/?demo=1">Try it with sample data</a><span>Opens four sample controls without changing a real page.</span></div><p class="desktop-note"><strong>Desktop Chrome and Chromium only.</strong> Mobile browsers can run the demo but cannot install this extension.</p><ul class="facts" aria-label="Product facts"><li>Page labels stay in your browser</li><li>Works offline after the first visit</li><li>Core actions are free; saved command names cost $12 once</li></ul></div><figure><img src="/art/hero.webp" width="1200" height="800" fetchpriority="high" decoding="async" alt="A printed browser page with a sound wave becoming a pointer." /><figcaption>Original generated artwork, made for Speak Page Actions.</figcaption></figure></section><section class="product-preview" aria-labelledby="preview-title"><div><p class="kicker">What the extension shows</p><h2 id="preview-title">Visible controls listed by the extension</h2><p>It lists buttons, links, and labelled fields already visible on the current page.</p></div><ol class="action-sample"><li><span>button</span> Save address</li><li><span>link</span> Review order</li><li class="warning"><span>review</span> Delete saved draft</li></ol></section><section class="steps" aria-labelledby="steps-title"><h2 id="steps-title">How Speak Page Actions works</h2><ol><li><strong>Open the extension.</strong><span>It scans the current page only.</span></li><li><strong>Hold to speak.</strong><span>Speak when your browser can process speech on this device, or type the visible control name.</span></li><li><strong>Review sensitive actions.</strong><span>It asks before submitting, deleting, publishing, sending, paying, canceling subscriptions, unsubscribing, archiving, deactivating, closing accounts, or signing out.</span></li></ol></section><section class="limits" aria-labelledby="limits-title"><h2 id="limits-title">What it does not do</h2><p>It does not listen in the background. It never lists password fields. It does not operate banking or financial pages.</p></section><section id="install" class="install" aria-labelledby="install-title"><h2 id="install-title">Install on desktop Chrome or Chromium</h2><p>Mobile browsers cannot install this extension. You can still use the demo on this device.</p><ol><li>Download and unzip the extension ZIP.</li><li>Open <code>chrome://extensions</code> on desktop Chrome or Chromium.</li><li>Turn on <strong>Developer mode</strong>.</li><li>Choose <strong>Load unpacked</strong>.</li><li>Select the unzipped extension folder.</li></ol><a class="button" href="/downloads/speak-page-actions.zip" download>Download the desktop extension ZIP</a></section><section class="price" aria-labelledby="price-title"><p class="kicker">One-time Pro license</p><h2 id="price-title">Save your own command names</h2><p>Pro costs $12 once. Save a short name for a visible control. Core page actions and safety checks are free.</p><a class="button primary" href="https://api.sociobot.in/api/v1/products/speak-page-actions/checkout">Buy Pro in hosted checkout — $12 once</a><p class="checkout-note">Opens Dodo’s checkout in this tab.</p><div class="restore-guide"><h3>Open the extension to restore Pro</h3><p>The website cannot turn on Pro inside your installed extension.</p><ol><li>Open the extension on a normal page.</li><li>Open <strong>Restore Pro and save command names</strong>.</li><li>Paste your token and choose <strong>Restore Pro</strong>.</li></ol><div id="license-handoff" role="region" aria-labelledby="license-handoff-title" tabindex="-1" hidden><h4 id="license-handoff-title">Move your license to the extension</h4><label for="returned-license">License token returned by checkout</label><div class="license-copy"><input id="returned-license" readonly autocomplete="off" spellcheck="false" /><button class="button" id="copy-license" type="button">Copy license token</button></div><p id="license-handoff-status" role="status" aria-live="polite">Copy this token, then paste it in the extension.</p></div></div></section></main>`);
  setupLicenseHandoff(returnedLicense);
}
function demo() {
  setMeta('Demo — Speak Page Actions', 'Try four visible controls without changing a real page.', '/demo');
  const state = JSON.parse(localStorage.getItem('demo:spa:sample') || '{"actions":[]}');
  const actions = state.actions.length ? state.actions : sampleActions;
  app.innerHTML = shell(`<div class="demo-bar" role="status">Demo — sample data, nothing is saved <button id="reset-demo">Reset demo</button><button id="leave-demo">Start for real</button></div><main id="main" tabindex="-1" class="demo-main"><p class="kicker">Sample checkout page</p><h1>Speak a visible control</h1><p class="lede">Hold the mic in the extension, or test the same command below.</p><section class="demo-console" aria-labelledby="demo-title"><div><h2 id="demo-title">Visible controls on this page</h2><p>Found four visible controls on Sample checkout.</p></div><ul id="demo-actions">${actions.map((action: DemoAction) => `<li><button data-action="${escapeHtml(action.label)}"><span>${action.kind}</span>${escapeHtml(action.label)}${action.destructive ? '<b> review</b>' : ''}</button></li>`).join('')}</ul><label for="demo-command">Type a command for the sample</label><div class="command"><input id="demo-command" placeholder="click save address" /><button id="run-demo" class="button primary">Run command</button></div><p id="demo-status" role="status" aria-live="polite">Found four visible controls on Sample checkout.</p><button id="demo-undo" class="button" hidden>Undo last page action</button></section><section class="demo-note"><h2>Sample data stays separate</h2><p>Reset restores four sample controls. Sample changes use separate browser storage and do not change extension settings.</p></section></main>`, 'demo');
  setupDemo(actions);
}
function legal(kind: 'privacy' | 'terms') {
  const privacy = kind === 'privacy'; setMeta(`${privacy ? 'Privacy' : 'Terms'} — Speak Page Actions`, privacy ? 'How Speak Page Actions handles browser-local data.' : 'Terms for Speak Page Actions.', `/${kind}`);
  app.innerHTML = shell(`<main id="main" tabindex="-1" class="legal"><p class="kicker">Speak Page Actions</p><h1>${privacy ? 'Privacy for your current page' : 'Terms for Speak Page Actions'}</h1>${privacy ? '<h2>What the extension reads</h2><p>The extension reads visible labels, roles, and control types on the current page after you open it. It does not read password fields.</p><h2>What stays on your device</h2><p>Saved command names and license data are stored in browser-local extension storage. The website does not store license tokens.</p><h2>What the extension sends</h2><p>The extension does not send page labels, spoken commands, or browsing history. When you restore Pro, it sends the license token to Sociobot for verification.</p><h2>Sensitive action review</h2><p>Review is required for submitting, deleting, publishing, sending, paying, canceling subscriptions, unsubscribing, archiving, deactivating, closing accounts, and signing out.</p><h2>Financial pages</h2><p>The extension does not operate banking or financial pages.</p><h2>Demo data</h2><p>The demo requests stay on this site. Its sample changes use separate browser storage.</p>' : '<h2>Using the extension</h2><p>Speak Page Actions helps operate ordinary pages. You remain responsible for reviewing and confirming each page action.</p><h2>License</h2><p>Pro is a one-time $12 license for saved command names.</p><h2>No warranty</h2><p>Browser pages change often. The extension may not identify every control or undo actions after a page navigates.</p>'}</main>`, kind);
}
function notFound() { setMeta('Page not found — Speak Page Actions', 'The page you requested could not be found.'); app.innerHTML = shell(`<main id="main" tabindex="-1" class="not-found"><p class="kicker">404</p><h1>This page could not be found</h1><p>The address may have changed. Return home to learn about visible page actions.</p><a class="button primary" href="/">Return home</a></main>`); }
function setupDemo(actions: DemoAction[]) {
  let last: DemoAction | undefined; const status = document.querySelector('#demo-status')! as HTMLElement;
  const run = (label: string) => { const target = label.toLowerCase().replace(/^(click|press)\s+/, ''); const action = actions.find((item) => item.label.toLowerCase() === target || item.label.toLowerCase().includes(target)); if (!action) { status.textContent = 'No visible action matched. Use a label from the list.'; return; } if (action.destructive && !confirm(`Review: “${action.label}” may change this sample page. Use it?`)) { status.textContent = 'The action was not used.'; return; } last = action; status.textContent = `Used ${action.label}.`; (document.querySelector('#demo-undo') as HTMLButtonElement).hidden = false; localStorage.setItem('demo:spa:sample', JSON.stringify({ actions })); };
  document.querySelector('#run-demo')?.addEventListener('click', () => run((document.querySelector('#demo-command') as HTMLInputElement).value)); document.querySelector<HTMLInputElement>('#demo-command')?.addEventListener('keydown', (event: KeyboardEvent) => { if (event.key === 'Enter') run((event.currentTarget as HTMLInputElement).value); }); document.querySelectorAll<HTMLButtonElement>('[data-action]').forEach((button) => button.addEventListener('click', () => run(button.dataset.action!))); document.querySelector('#demo-undo')?.addEventListener('click', () => { status.textContent = last ? `Undid ${last.label} in the sample.` : 'There is no sample action to undo.'; (document.querySelector('#demo-undo') as HTMLButtonElement).hidden = true; }); document.querySelector('#reset-demo')?.addEventListener('click', () => { for (const key of Object.keys(localStorage)) if (key.startsWith('demo:spa:')) localStorage.removeItem(key); location.reload(); }); document.querySelector('#leave-demo')?.addEventListener('click', () => { for (const key of Object.keys(localStorage)) if (key.startsWith('demo:spa:')) localStorage.removeItem(key); navigate('/#install'); });
}
function setupLicenseHandoff(token: string | null) {
  if (!token) return;
  const handoff = document.querySelector<HTMLElement>('#license-handoff');
  const field = document.querySelector<HTMLInputElement>('#returned-license');
  const status = document.querySelector<HTMLElement>('#license-handoff-status');
  const copy = document.querySelector<HTMLButtonElement>('#copy-license');
  if (!handoff || !field || !status || !copy) return;
  handoff.hidden = false;
  field.value = token;
  history.replaceState(history.state || {}, '', `${location.pathname}${location.hash}`);
  requestAnimationFrame(() => {
    handoff.scrollIntoView({ block: 'center' });
    handoff.focus({ preventScroll: true });
  });
  copy.addEventListener('click', async () => {
    try {
      await navigator.clipboard.writeText(token);
      status.textContent = 'License token copied. Paste it in the extension.';
    } catch {
      field.focus();
      field.select();
      status.textContent = 'Copy the selected token, then paste it in the extension.';
    }
  });
}
const pages: Record<string, () => void> = { '/': home, '/demo': demo, '/privacy': () => legal('privacy'), '/terms': () => legal('terms'), '/404': notFound };
type ScrollState = { scrollX?: number; scrollY?: number };
function saveScroll() { history.replaceState({ ...(history.state || {}), scrollX: window.scrollX, scrollY: window.scrollY }, '', location.href); }
function navigate(path: string) { saveScroll(); history.pushState({ scrollX: 0, scrollY: 0 }, '', path); render(true); requestAnimationFrame(() => { const hash = new URL(path, location.origin).hash; if (hash) document.querySelector(hash)?.scrollIntoView(); else window.scrollTo(0, 0); }); }
function render(moveFocus = false, state: ScrollState = {}) { (pages[route()] || notFound)(); requestAnimationFrame(() => { const heading = document.querySelector('h1') as HTMLElement; heading?.setAttribute('tabindex', '-1'); if (moveFocus) heading?.focus({ preventScroll: true }); document.querySelector('#route-note')!.textContent = document.title; if (typeof state.scrollY === 'number') window.scrollTo(state.scrollX || 0, state.scrollY); }); }
history.scrollRestoration = 'manual'; saveScroll();
document.addEventListener('click', (event) => { const link = (event.target as HTMLElement).closest('a[href]') as HTMLAnchorElement | null; if (!link || link.origin !== location.origin || link.hasAttribute('download')) return; event.preventDefault(); navigate(`${link.pathname}${link.search}${link.hash}`); }); window.addEventListener('popstate', (event) => render(true, event.state || {})); if ('serviceWorker' in navigator) navigator.serviceWorker.register('/service-worker.js').catch(() => undefined); render();
